# Production API URL (update this with your actual Fly.io URL)
API_BASE_URL=https://tipsyclub.org

# SMS Secret Token (must match the one set in Fly.io)
SMS_SECRET_TOKEN=poker-sms-secret-2025-secure-token

# Polling interval in milliseconds (how often to check for new SMS messages)
POLL_INTERVAL=30000

# Enable debug logging
DEBUG=true
